:2000

# Forward /mercury/* to the sidecar (strips prefix before forwarding)
handle_path /mercury/* {
	reverse_proxy mercury-proxy:3001
}

# Serve the ReactFlux SPA
handle {
	root * /srv
	try_files {path} {path}/ /index.html
	file_server
}
